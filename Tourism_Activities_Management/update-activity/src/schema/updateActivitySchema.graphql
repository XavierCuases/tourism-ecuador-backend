type Query {
  _empty: String
}

type Mutation {
  updateActivity(id: ID!, name: String, location: String, date: String): Activity
}

type Activity {
  id: ID!
  name: String
  location: String
  date: String
}
