type Query {
  _empty: String
}

type Mutation {
  createActivity(name: String!, location: String!, date: String!): Activity
}

type Activity {
  _id: ID!
  name: String!
  location: String!
  date: String!
}
